name: Text with image
icon: text
label: "| {{ contenttitle }}"
fields:
  textWithImage:
    extends: fields/textWithImage
    fields:
      contentHeadline:
        numbered: false
      contentSwitch: false