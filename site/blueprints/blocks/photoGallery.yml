name: Photo gallery
icon: dashboard
label: "| {{ photogallerytitle }}"
fields:
  photoGallery:
    extends: fields/photoGallery
    fields:
      photoGalleryHeadline:
        numbered: false
      photoGallerySwitch: false